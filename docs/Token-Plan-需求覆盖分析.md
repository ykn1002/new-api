# Token Plan 需求 × new-api 现状 覆盖分析

> 目的：评估《Token Plan 方案》需求，判断 new-api 现有能力能覆盖哪些、哪些需改造或新建。
>
> 结论速览：**计费内核（人民币→额度→Token）、模型定价、充值/支付、流水、统计看板、限流、代充值** 这些基础设施 new-api 已具备，多数可直接复用或小改；**真正的缺口集中在：积分有效期、积分来源分账、带赠送积分的充值档位、默认模型强制、微信支付接口、小程序账号一对一映射/自动开户、运营看板的导出与总览聚合**。

## 图例

- ✅ 已支持，可直接用
- 🟡 部分支持，需改造/补齐
- ❌ 不支持，需新建
- **既定口径：方案里的「积分」≡ new-api 的「自定义货币」（`QuotaDisplayType=CUSTOM`）。** new-api 内部计费单位是 **quota（整数高精度台账）**，用户不直接感知；积分是 quota 的对外展示投影：`积分 = (quota / QuotaPerUnit) × CustomCurrencyExchangeRate`。扣费/精度都在 quota 层完成，对外一律以积分呈现。

---

## 〇、技术栈（new-api 现状）

| 层 | 技术 |
|---|---|
| 后端 | Go 1.22+、Gin（HTTP 框架）、GORM v2（ORM） |
| 前端 | 默认主题 `web/default`：React 19 + TypeScript + Rsbuild + Base UI + Tailwind CSS；经典主题 `web/classic`：React 18 + Vite + Semi Design |
| 前端包管理 | Bun（优先于 npm/yarn/pnpm） |
| 数据库 | SQLite / MySQL ≥5.7.8 / PostgreSQL ≥9.6（三者须同时兼容） |
| 缓存 | Redis（go-redis）+ 进程内内存缓存 |
| 鉴权 | JWT、WebAuthn/Passkeys、OAuth（GitHub/Discord/OIDC 等） |
| 国际化 | 后端 `nicksnyder/go-i18n/v2`（en/zh）；前端 `i18next`（zh/en/fr/ru/ja/vi） |

**分层架构**：`Router → Controller → Service → Model`，AI 转发在 `relay/`（各厂商适配器 `relay/channel/*`）。本方案改造主要落在 `model/`（积分批次、价格计划）、`service/`（扣减分摊、预警）、`controller/`+`router/`（积分/支付/小程序接口）、`setting/`（汇率/档位/默认模型配置）、`middleware/distributor.go`（默认模型强制）。

> 约束（详见 `AGENTS.md`）：JSON 统一走 `common.Marshal/Unmarshal`；DB 代码须三库兼容（优先 GORM 抽象，慎用方言）；前端用 Bun；不得改动 new-api / QuantumNous 标识。

---

## 一、总体概念映射

| 方案概念 | new-api 对应 | 状态 | 说明 |
|---|---|---|---|
| 积分（对外展示单位） | **自定义货币展示层** `QuotaDisplayType=CUSTOM`（`general_setting.go`），底层仍是 `User.Quota` | ✅ | 展示公式：`积分 = (quota / QuotaPerUnit) × CustomCurrencyExchangeRate`。扣费/精度走 quota，对外只看积分 |
| Tokens | 日志 `prompt_tokens` / `completion_tokens`（`model/log.go`） | ✅ | 已逐次记录 |
| 充值汇率（1 元 = 100 积分） | `CustomCurrencyExchangeRate`（锚定美元）+ `USDExchangeRate` | 🟡 | **推荐**：不使用内置支付渠道时设 `USDExchangeRate=1`（人民币≡美元），则 `CustomCurrencyExchangeRate=100` 即「1元=100积分」。详见需求 2 的「推荐方案」 |
| 模型计费规则 | `ModelRatio`（输入）+ `CompletionRatio`（输出） | ✅ | 已区分输入/输出 |
| 充值档位 | `PaymentSetting.AmountOptions` + `AmountDiscount` | 🟡 | 有金额档位+折扣，但无「档位名称+固定赠送积分」 |
| 套餐 | `SubscriptionPlan`/`UserSubscription` 订阅系统 | ✅ | 后台配置/管理（CRUD、上下架、有效期、赠送积分、运营发放 `AdminBindSubscription`）**本期做且已支持**；仅「用户端付费购买」本期不开放（待确认第5条） |
| 流量池 | `QuotaData` 看板（`model/usedata.go`）+ 日志统计 | 🟡 | 有统计，缺部分管控与导出 |
| 积分账户 | `User.Quota`（底层余额）/ `UsedQuota`（累计消耗），对外按积分展示 | 🟡 | 账户存在：余额、累计消耗已覆盖（底层 quota，展示时换算为积分）；**缺「按来源分账(赠送/套餐/充值)」与「有效期」**（见缺口 A-1、A-2） |
| 积分流水 | `Log`（topup/consume/refund/manage 类型） | ✅ | 完整台账 |

---

## 二、逐条需求覆盖

### 需求 1 / 模块 3.1：模型 Tokens–积分换算设置

| 功能点 | 状态 | 现状 / 差距 |
|---|---|---|
| 模型列表、计费规则与状态展示 | ✅ | 后台倍率/价格配置页已存在（`controller/pricing.go`、`controller/ratio_config.go`） |
| 新增模型并配置单价 | ✅ | `ModelRatio` / `ModelPrice` 可后台增改 |
| 区分输入 / 输出 Token 单价 | ✅ | 输入用 `ModelRatio`，输出用 `CompletionRatio`（输出价=模型倍率×补全倍率） |
| 编辑 / 启停模型、下线后不可调用 | 🟡 | 模型注册表 `Model.Status`（`model/model_meta.go`）可启用/停用（偏展示/市场层）；但**调用是否放行由渠道 `Channel.Status` + 能力 `Ability` 决定**，未见 `Model.Status` 参与路由门禁。「下线即不可调用」需经渠道/能力实现，或补 `Model.Status` 路由校验 |
| 规则生效时间（定时生效） | ❌ | docx 3.1 功能点与字段表（示例 `2026-06-15`）要求「可配置规则生效时间，便于调价过渡」。new-api 改价**立即生效，无预约定时生效**机制，需新建。注：此项与待确认第7条「调价不回算存量」是两回事，不可混同 |
| 单次消耗积分保留两位小数 | ✅ | 前端自定义货币展示默认 `digitsLarge=2`（≥1 的值即 2 位小数），**基本已满足**。仅边角差异：小额(<1)显示 4 位、大额(≥1000)缩写为 k、后端日志文本 `logger.go` 为 `%.6f`——如需严格统一可微调，非阻塞 |

> 说明：方案示例「输入 2 元/1K、输出 6 元/1K」可用 `ModelRatio`+`CompletionRatio` 表达。配置时先按"积分=自定义货币"标定好汇率（见需求 2），扣减的 quota 再投影成积分展示即可。

> ✅ 概念澄清（docx 1.4「关键换算关系」+ 3.6 看板）：此处「成本/成本估算」**不是上游渠道的真实成本，而是按计费规则（售价口径）把所有消耗 Token 折算出的总金额**——即「所有 token 的总钱数」。docx 1.4 的「tokens→人民币」是用模型倍率（`ModelRatio`/`CompletionRatio`）算出的金额，3.6 看板的「成本估算」就是这些消耗金额的汇总。new-api 已逐次按 `quota` 记录消耗，聚合 `QuotaData.Quota` 后按汇率换算成金额即可直接得出，**无需接入上游真实成本，无需独立成本核算**。结论：✅ 已具备能力，归入需求 5 看板聚合（见下）。

### 需求 2 / 模块 3.2：充值汇率与「人民币→积分→Tokens」计费链路

| 功能点 | 状态 | 现状 / 差距 |
|---|---|---|
| 人民币→积分→Tokens 计费链路 | ✅ | 内核已是 `钱 × 汇率 = quota`，扣费 `tokens × 模型倍率 = quota`，积分为 quota 展示投影，链路同构 |
| 全局充值汇率（默认 1 元=100 积分，可配） | 🟡 | 用「自定义货币展示」承载积分。**推荐「人民币≡美元」**：`USDExchangeRate=1` + `QuotaDisplayType=CUSTOM` + `CustomCurrencySymbol="积分"` + `CustomCurrencyExchangeRate=100`，即「1元=100积分」。自建微信支付直接加 quota，无需走 `getPayMoney`（详见下方「推荐方案」） |
| 仅研发可配置汇率 | 🟡 | 自定义货币/汇率属系统设置，运营后台可改，需收紧到「研发/超管」权限 |
| Tokens 与积分关系仅在模型计费配置 | ✅ | 已分离：充值环节只管「钱→quota」，扣费在模型倍率，积分只是展示 |

### 需求 / 模块 3.2、4.1：充值档位配置

| 功能点 | 状态 | 现状 / 差距 |
|---|---|---|
| 自由金额充值 | ✅ | `AmountOptions` + 自定义金额 |
| 固定充值档位（金额→基础积分） | 🟡 | 现为「金额档 + 折扣率」，**没有「档位名称 + 固定赠送积分」字段**，需扩展档位数据结构 |
| 档位赠送积分 | 🟡 | 现靠折扣间接实现，需显式「赠送积分」字段 |
| 不提供套餐购买、不限购买次数 | ✅ | 可直接不启用订阅；充值次数本就不限 |
| 充值订单生成 + 支付回调 + 到账 + 写流水 | ✅ | `model/topup.go` 全流程已实现（含 Stripe/Creem/Waffo/易支付） |

#### 推荐方案：人民币 ≡ 美元（不使用内置支付渠道时）

如果**不使用内置支付渠道**（Stripe/易支付等），自建微信支付直接给用户加 quota，则可把内部的「USD」当作人民币使用，汇率配置回归直觉：

```
USDExchangeRate            = 1      // 原 7.3；设 1 后「1 美元单位」≡ 1 元
QuotaPerUnit               = 500000 // 保持默认（倍率 1 = 每 token 扣 1 quota，最整）
QuotaDisplayType           = CUSTOM
CustomCurrencySymbol       = "积分"
CustomCurrencyExchangeRate = 100    // 1 元 = 100 积分（不再需要 ×7.3=730）
```

**全链路验证（用方案 3.1 的算例）：**

- 充值：付 1 元 → 加 `1 × 500000 = 500000` quota → 展示 `(500000/500000)×100 = 100 积分` ✅
- 模型 A 定价（输入 2 元/1K、输出 6 元/1K）：
  - 输入每 token = `0.002 × 500000 = 1000` quota → `ModelRatio = 1000`
  - 输出是输入的 3 倍 → `CompletionRatio = 3`
- 扣费（输入 1500 + 输出 800）：
  - quota = `1500×1000 + 800×1000×3 = 3,900,000`
  - 折算 = `3,900,000 / 500000 = 7.8 元` → **780 积分**，与方案「扣减 7.8 元」算例自洽 ✅

**注意事项：**

1. 关闭/不使用内置支付渠道（它们走 `Price=7.3` 的美元换算）；自建微信支付回调按 `quota = 付款元数 × QuotaPerUnit` 加额度。
2. 内置默认模型倍率是按上游美元成本调的，基准当人民币后会失真——需按自己产品给「默认模型」重配倍率（本就要做）。
3. 不要再用 USD 展示类型（会把「$」显示成实际人民币值），统一用 CUSTOM（积分）展示。
4. `QuotaPerUnit` 可自选，但 500000 让「倍率=每 token quota」最整，建议保持；若改需同步重算上面所有换算。

### 需求 3 / 模块 3.3：用户额度、充值、积分有效期、来源分账

| 功能点 | 状态 | 现状 / 差距 |
|---|---|---|
| 额度展示（余额、累计消耗、有效期） | 🟡 | 积分余额（底层 `Quota` 换算）、累计消耗（`UsedQuota`）有；**有效期没有** |
| 余额预警（低于阈值提示、为 0 拦截） | 🟡 | 余额不足在预扣阶段拦截（`service/quota.go`）✅；但阈值是**绝对值**（`QuotaRemindThreshold`/用户级 `QuotaWarningThreshold`），docx 3.3 要的是**百分比阈值（剩余 < 20%，可配）**，需新增按余额百分比触发的逻辑 |
| 预警通知（通知用户 + 通知运营） | 🟡 | 通知用户已支持（邮件 / Webhook / Bark / Gotify）；docx 4.3 还要求「**同时通知运营**」，缺面向运营的低余额通知通道，需补 |
| 用户自助充值、实时到账 | ✅ | 已支持（Web 端，非小程序） |
| 运营后台代充值（含备注、操作人、流水） | ✅ | `ManageUser`→`IncreaseUserQuota`、`ManualCompleteTopUp`，并写审计日志 |
| **积分设有效期、到期清零** | ❌ | **积分余额（底层 quota）无有效期、不会过期**。仅兑换码/Token/订阅有到期。需新建积分有效期机制 |
| **多次购买有效期顺延** | ❌ | 无有效期即无顺延，需配合上一条新建 |
| **积分来源分账（赠送/套餐/充值）+ 优先扣减顺序** | ❌ | 底层为单一整数 quota 余额。三来源（赠送/套餐/充值），赠送优先扣→套餐→充值 的分层扣减需新建「积分批次/分账户」模型。本期套餐积分仅经后台配置/运营发放产生（无用户购买入口） |
| 来源构成卡片（赠送/套餐/充值占比） | ❌ | 依赖上面的分账模型，需新建 |
| 去向流水概览（已消费/已过期） | 🟡 | 已消费可从日志统计；「已过期」依赖有效期机制（待建） |
| 积分明细流水表（时间/类型/来源/变动/余额/备注 + 类型筛选） | 🟡 | 日志有时间/类型/变动/备注，**缺「来源」「操作后余额」列**，需补字段 |

### 需求 / 模块 3.4：套餐管理（本期做后台配置，不开放用户购买）

| 功能点 | 状态 | 现状 / 差距 |
|---|---|---|
| 套餐增删改查、上下架、排序、有效期、赠送积分 | ✅ | `SubscriptionPlan` 订阅系统已完整支持 |
| 周期续费 / 重置 | ✅ | `QuotaResetPeriod`（daily/weekly/monthly/custom） |
| 购买上限 | ✅ | `MaxPurchasePerUser` |
| 运营为用户发放套餐 | ✅ | `AdminBindSubscription`（后台直接绑定，无需支付） |

> 注：待确认第 5 条否掉的是**用户端付费购买套餐**（本期只开放积分档充值）；**套餐的后台配置/管理与运营发放属本期范围且已支持**（原需求模块 3.4、第 5 节运营后台功能清单均列入）。即「能配、能由运营发，不能让用户自己买」。

### 需求 4 / 模块 3.5：小程序「我的」积分充值（**小程序端已自建，仅需后端提供接口**）

> 前提更新：微信小程序客户端已存在，本期 new-api 只需**对外提供接口**，不涉及小程序 UI 开发。下面只评估「后端接口」缺口。

| 接口 / 能力 | 状态 | 现状 / 差距 |
|---|---|---|
| 小程序登录鉴权（code2session 换 openid → 签发本系统 token） | ✅ | 已有小程序登录接口可用 |
| **小程序账号与本系统用户一对一映射 + 自动开户** | 🟡 | 现有 `UserOAuthBinding`（`(provider_id, provider_user_id)` 唯一→user_id）可复用，无需建表；需补「按 `mp_account_id`(unionid/openid) 查映射，命中放行、未命中自动建 User+绑定」逻辑与并发去重、信任边界校验（详见技术方案 M7） |
| 积分概览（余额 / 有效期 / 当前套餐） | 🟡 | 余额接口可复用（`/api/user/self`）；**有效期字段依赖积分有效期机制（缺口 A）**；「当前套餐」本期不做套餐，可省略 |
| 充值档位列表 | ✅ | `GetTopUpInfo`（`controller/topup.go`）可复用，档位结构待按缺口 B-3 扩展 |
| 下单 + 支付 | 🟡 | 下单链路具备，但**无原生微信支付（JSAPI/小程序支付）渠道**。现有 Stripe/Creem/Waffo/易支付；需新增微信支付渠道（小程序下单返回 `prepay_id` + 签名参数） |
| 支付回调 → 积分到账 | 🟡 | 回调框架具备（参考各 `topup_*.go`），需补微信支付回调验签与到账 |
| 积分明细（充值/消耗/赠送/过期，按类型/时间筛选） | 🟡 | 日志查询接口可复用，**缺「来源」列与「过期」类型（依赖缺口 A）** |
| 余额不足提醒 | ✅ | 预警阈值 + 余额不足拦截已具备，接口返回可携带 |

### 需求 5 / 模块 3.6：流量池运营看板

| 功能点 | 状态 | 现状 / 差距 |
|---|---|---|
| 总览仪表盘（总 Token、总积分、成本估算、累计充值、用户数） | 🟡 | `QuotaData` 看板 + 日志统计具备大部分（其中 `QuotaData.Quota` 是底层 quota，展示「总积分」时按汇率换算）；**「成本估算」= 所有消耗 Token 按售价折算的总金额**（聚合 `QuotaData.Quota` 换算即得，非上游成本，已具备）；「累计充值金额、活跃用户数」等聚合可能需补 |
| 多维统计（时间/模型/用户 趋势与排行） | ✅ | `model/usedata.go`、`controller/rankings.go` 已支持按用户/模型/时间聚合 |
| 用户明细 + 下钻消耗流水 | ✅ | 已有按用户查询与日志下钻 |
| 模型明细（调用次数/Token/积分占比） | ✅ | `GetAllQuotaDates` 按模型聚合 |
| 数据导出（Excel/CSV） | ❌ | **未发现服务端日志/统计的 Excel/CSV 导出**，需新建（或前端导出） |
| 管控：总流量上限/告警阈值 | ❌ | 无平台级 Token 总量上限/告警，需新建（待确认第8条说「本期不做流量管控」） |
| 管控：单用户单位时间限额（每日 Token/积分） | 🟡 | 有按分组的请求频率限流（`setting/rate_limit.go`），但**无「每日 Token/积分额度」式限额** |
| 管控：一键暂停某/全部模型 | 🟡 | 通过禁用渠道实现，无「单模型一键暂停」开关 |
| 管控：黑/白名单 | 🟡 | 有邮箱域名白名单、SSRF 黑白名单、Token IP 白名单，无「异常用户黑/白名单」专用功能 |

> 方案「待确认第 8 条」：本期**只做监控统计，不做流量管控**。若按此，上面的管控类缺口本期可不实现，仅需补「导出」和「总览聚合」。

### 需求 6 / 待确认第 9 条：默认模型、用户不可选模型

| 功能点 | 状态 | 现状 / 差距 |
|---|---|---|
| 设置一个默认模型 | ❌ | 无「全局默认模型」配置项，需新建 |
| 用户不可自行选择模型 | 🟡 | 可用 Token 级 `ModelLimits` 白名单 / 分组可用模型间接限制，但**没有「强制单一默认模型、隐藏选择」的开关**，需新建 |

---

## 三、缺口汇总（按工作量与优先级）

### A. 必须新建（核心缺口）

1. **积分有效期 + 到期清零 + 多次充值顺延**（需求 3）——wallet quota 当前无有效期，需引入「积分批次」模型（批次=金额/有效期/剩余/来源）。
2. **积分来源分账（赠送/套餐/充值）+ 优先扣减顺序**（需求 3）——配合批次模型实现「赠送优先→套餐→充值」扣减与占比展示。本期套餐积分经后台配置/运营发放产生（无用户购买入口）。
3. **充值档位扩展**：从「金额+折扣」升级为「档位名称 + 金额 + 基础积分 + 赠送积分 + 有效期」（需求 2）。
4. **全局默认模型 + 禁止用户选模型**（需求 6）。
5. **微信支付接口（小程序登录已具备）**：微信支付渠道（小程序 JSAPI 下单返回 `prepay_id` + 签名参数）+ 回调验签到账。复用现有登录/余额/档位/明细接口。
6. **小程序账号一对一映射 + 自动开户**（需求 4）：复用 `UserOAuthBinding`，补「按 `mp_account_id` 查映射→命中放行/未命中自动建 User+绑定」+ 并发去重 + 信任边界校验（详见技术方案 M7）。
7. **模型规则定时生效（调价预约）**（需求 1 / docx 3.1）——支持配置规则「生效时间」，到点自动切换价格；现状改价即时生效，需新建。

### B. 改造 / 包装（已有内核，需适配）

8. **启用「积分」自定义货币展示 + 对齐充值汇率**：推荐「人民币≡美元」简化（`USDExchangeRate=1`、`QuotaDisplayType=CUSTOM`、符号「积分」、`CustomCurrencyExchangeRate=100` 即 1元=100积分），自建微信支付直接加 quota。无需改动 quota 内核（需求 2，详见该节「推荐方案」）。
9. **积分两位小数**（基本已满足，可选微调）：前端自定义货币默认 ≥1 即 2 位小数；仅小额(<1)4位、大额缩写 k、后端日志 `%.6f` 三处边角，如需严格统一再调（需求 1）。
10. **流水表补字段**：来源(source)、操作后余额(balance_after) 列 + 类型筛选（需求 3）。
11. **运营看板补「导出 + 总览聚合（累计充值/活跃用户）」**（需求 5）。
12. **汇率配置权限收紧到研发/超管**（需求 2）。
13. **余额预警改造**：①阈值从绝对值扩展为支持**百分比（剩余<20%，可配）**；②低余额除通知用户外，**同时通知运营**（需求 3 / docx 3.3、4.3）。

### B'. 待澄清（评审确认后再定）

- （本节暂无）原「独立成本核算」一项已澄清：看板「成本估算」是「所有 Token 按售价折算的总金额」，非上游真实成本，聚合现有消耗数据即可得出，归入 B-11 看板总览聚合，无需独立成本建模。

### C. 已具备，可直接用

- 模型输入/输出分别计费（`ModelRatio` + `CompletionRatio`）。
- 充值订单 + 支付回调 + 到账 + 流水（多支付渠道）。
- 余额不足拦截 + 通知用户（邮件/Webhook/Bark/Gotify）——注：百分比阈值与通知运营见 B-13。
- 运营后台代充值 + 审计日志（注：现按「额度数值」代充，docx 期望「按档位」代充，差异较小）。
- 多维统计看板（用户/模型/时间）+ 下钻。
- 限流体系（分组请求频率）。
- 订阅/套餐系统：后台配置/管理 + 运营发放（本期做，已具备；仅不开放用户付费购买）。

---

## 四、本期明确不做（排除范围）

依据方案「待确认问题」的回复，以下能力**本期不实现**，需求评审时确认无误。new-api 现状即使部分缺失也无需补齐。

| # | 不做项 | 依据（待确认问题回复） | 现状说明 |
|---|---|---|---|
| 1 | **流量管控**：总量上限/告警、单用户每日限额、模型暂停、用户黑/白名单 | 第 8 条「不做流量管控」 | 本期只做监控统计；仅需补「数据导出 + 总览聚合」 |
| 2 | **套餐用户购买**（用户端付费购买套餐） | 第 5 条「不提供套餐购买，只提供积分档，不限次数」 | 仅关闭用户购买入口；**套餐后台配置/管理与运营发放本期保留且已支持**（订阅系统现成） |
| 3 | **退款 / 回收已发放积分** | 第 6 条「不支持退款」 | 不实现退款链路 |
| 4 | **发票 / 对账** | 第 10 条「无发票对账要求」 | — |
| 5 | **调价对存量的处理**（存量积分、进行中订单重算） | 第 7 条「不做处理」 | 改价立即生效，不回溯存量 |
| 6 | **用户侧展示 Token 数** | 第 4 条「仅展示积分」 | 对外只展示积分，Token 仅平台内部统计 |

---

## 五、覆盖度评估与总结

**量化口径**：统计本期范围内（已排除「四、本期明确不做」的流量管控、套餐购买等）约 45 个功能点，按 ✅=1 / 🟡=0.5 / ❌=0 计分。

| 类别 | 数量 | 占比 | 说明 |
|---|---|---|---|
| ✅ 完全可用 | 约 21 项 | ~47% | 模型输入/输出定价、充值订单+多渠道支付、自助/代充值、套餐后台配置、多维统计看板、限流等 |
| 🟡 部分支持/小改即可 | 约 17 项 | — | 积分自定义货币展示与汇率对齐、充值档位扩展、小程序下单/映射、流水补字段、百分比预警等 |
| ❌ 需新建 | 约 7 项 | ~16% | 积分有效期+顺延、来源分账+来源卡片、全局默认模型、规则定时生效、数据导出 |

**加权覆盖 ≈ 65%**（= (21 + 17×0.5) / 45）。

**结论**：new-api 的**计费内核与运营后台基础设施**已覆盖约 65% 的本期能力，且大量 🟡 项只是「在已有内核上做语义包装/字段补齐」，真正从零新建的硬缺口约 16%，**集中在「积分产品化账本」**——即积分有效期、到期清零顺延、按来源分账与优先扣减（需引入「积分批次」模型，技术方案 M2）。

其余重点新建项：全局默认模型/禁止选模型、微信支付接口、小程序账号一对一映射/自动开户、模型规则定时生效、看板数据导出。配置/包装类（积分=自定义货币、人民币≡美元、两位小数）成本低。看板「成本估算」已澄清为「消耗 Token 按售价折算的总金额」，聚合现有数据即可，无独立成本核算需求。

---

## 六、附录：主流国产模型 Token 计算口径

> 结论先行：new-api 对国产模型一律**以上游官方返回的 `usage` 为准计费**，不在本地用 tiktoken 重算；仅当上游未返回 usage（极少数流式场景）才本地兜底估算。计费换算与模型无关：`quota =（输入 + 输出 × CompletionRatio）× ModelRatio × GroupRatio`。

| 模型 | 渠道 | 响应处理 | Token 来源 |
|---|---|---|---|
| 文心一言 ERNIE | `baidu` | 专用 handler（`baidu/relay-baidu.go` `baiduHandler`/`baiduStreamHandler`）解析百度原生 `usage`→标准 `dto.Usage` | 上游返回的 `usage`（流式取 `TotalTokens`，补全=总-输入） |
| 文心一言（V2 接入） | `baidu_v2` | OpenAI 兼容，复用 `openai.Adaptor.DoResponse` | 上游 `usage` |
| 火山/豆包 Doubao | `volcengine` | OpenAI 兼容（`/api/v3/chat/completions`），复用 `openai.Adaptor.DoResponse` | 上游 `usage` |
| 千问 Qwen | `ali` | 文本走 `openai.Adaptor.DoResponse`；流式主动设 `stream_options.include_usage=true` | 上游 `usage`（原生 `AliUsage{input_tokens,output_tokens}` 已规整） |
| 智谱 GLM | `zhipu` / `zhipu_4v` | 专用 handler（`zhipu/relay-zhipu.go`）解析智谱原生 `usage`→标准 `dto.Usage`；若走 OpenAI 兼容端点则同火山/千问 | 上游 `usage` |
| MiniMax | `minimax` | 文本走 `openai.Adaptor.DoResponse`（TTS 按 `usage_characters`，图像特殊） | 上游 `usage` |
| 讯飞星火 Spark | `xunfei` | 专用 WebSocket handler（`xunfei/relay-xunfei.go`），从 `payload.usage.text` 累加 prompt/completion/total | 上游 `usage` |
| DeepSeek | `deepseek` | OpenAI 兼容，复用 `openai.Adaptor.DoResponse`（Claude 格式走 claude 适配器） | 上游 `usage` |

**本地兜底估算**（仅上游缺 usage 时）：
- 输入：请求前 `EstimateRequestToken`（`service/token_counter.go`）预估的 prompt token；
- 输出：`EstimateTokenByModel`（`service/token_estimator.go`）——以上模型名均不含 `gemini`/`claude`，故走 **OpenAI 风格字符估算**（非 tiktoken 精确编码）。

**对积分计费的意义**：token 数由上游官方计量，准确且无需改造；接入新模型时只需为其配置 `ModelRatio`（输入元价/1K）与 `CompletionRatio`（输出/输入价比），即可得到正确的积分扣减。